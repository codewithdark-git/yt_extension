from fastapi import FastAPI

app = FastAPI(title="YouTube Content Assistant API", 
             description="API for blog generation and question answering from YouTube videos",
             version="1.0.0")
